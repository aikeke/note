1.虚拟化技术分类
技术角度分类：
  全虚拟化，在虚拟机和硬件之间加了一个软件层--hypervisor（VMM），kvm直接运行在硬件之上，QEMU和WINE的vmm运行在另外一个os上
  半虚拟化，在客户端os和vmm之间增加一个api，优化指令，减小vmm工作负担，性能有很大提高。（难度大，成本大，用的不多）
架构角度分类：
  寄居架构，依赖宿主机，简单便于实现，资源浪费，且依赖主机os的支持，如：VMware server，Workstation
  裸金属架构，直接工作在硬件上，灵活，稳定，但虚拟层内核开发难度大，如：Vmware ESXI Server
